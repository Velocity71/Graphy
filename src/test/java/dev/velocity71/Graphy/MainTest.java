package dev.velocity71.Graphy;

//import static org.junit.jupiter.api.Assertions.assertNotNull;
//import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

import java.io.IOException;

/**
 * Unit tests for the Main.java class.
 *
 * @since 0.2.10
 * @author Velocity71
 * @version 0.2
 */
public class MainTest {

    @Test
    public void testLoadConfig() throws IOException {
    	//Main main = new Main();

        //main.main(new String[0]);

        //assertNotNull(
        //    main.getConfig(),
        //    "Main.getConfig() returned a null value."
        //);

        // Assert that all the data in the config file is accessable and correct.
        //assertEquals(main.getConfig().getProperty("app.fxml.path"), "fxml/app.fxml");
        //assertEquals(main.getConfig().getProperty("logs.path"), "logs/");
    }
}
